
# both of these are considered strings
sample1 = 'test'
sample2 = "test"

# strings in python are encoded
# they are stored as numbers
# enter character and space characters are
# represented by special designated numbers
# the most widely used encoding in the word is ascii
# there are 256 ascii numbers
# for additional languages, code pages are used.

# for python we can use unicode
# unicode stores 4 times the amount of data. That is because characters are 32 bits not 8 bits
# utf-8 is from 8 to 32 bits. So it can be smaller than ascii.